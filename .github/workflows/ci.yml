name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: lint + test + build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
          targets: wasm32-wasip1

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: rustfmt
        run: cargo fmt --all -- --check

      # `-- -D warnings` keeps the lint-deny scoped to our crates; using
      # `RUSTFLAGS=-D warnings` would also break the build on any
      # deprecation warning in a transitive dependency.
      - name: clippy (native crates)
        run: cargo clippy --locked --workspace --exclude zellij-linear-plugin --all-targets -- -D warnings

      - name: clippy (plugin, wasm32-wasip1)
        run: cargo clippy --locked -p zellij-linear-plugin --target wasm32-wasip1 -- -D warnings

      - name: test (native crates)
        run: cargo test --locked --workspace --exclude zellij-linear-plugin

      - name: test (plugin, host target)
        run: cargo test --locked -p zellij-linear-plugin

      - name: build native CLI (release)
        run: cargo build --locked --release -p zellij-linear

      - name: build wasm plugin (release)
        run: cargo build --locked --release --target wasm32-wasip1 -p zellij-linear-plugin

      - name: upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: zellij-linear-${{ github.sha }}
          path: |
            target/release/zellij-linear
            target/wasm32-wasip1/release/zellij-linear-plugin.wasm
          if-no-files-found: error
          retention-days: 14
